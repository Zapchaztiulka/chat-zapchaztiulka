import tailwindcss from '../../tailwind.config';

export const iconColors = tailwindcss.theme.extend.colors.iconColors;
export const bgColors = tailwindcss.theme.extend.colors.bgColors;
export const textColors = tailwindcss.theme.extend.colors.textColors;
export const mainColors = tailwindcss.theme.extend.colors.mainColors;
